[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "cde-led-dev"
version = "0.1.0"
description = "LED matrix helpers for Copper Dragons"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["cde_led_dev"]
