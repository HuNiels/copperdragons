[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "rgb-matrix"
version = "0.1.0"

[tool.setuptools]
package-dir = {"" = "bindings/python/samples"}

[tool.setuptools.packages.find]
where = ["bindings/python/samples"]